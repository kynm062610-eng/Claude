# Supabase の接続情報。プロジェクト作成後に控えてコピーする。
# EXPO_PUBLIC_ が付いた環境変数はアプリのバンドルに埋め込まれる。
# anon key はクライアントに出る前提のキーなので問題ないが、
# service_role キーは絶対にここに書かないこと（RLS を無視できるため）。
EXPO_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
