# 第二の脳

Claude（クラウド版）が「保存して」って言われた時に作ったリサーチ・分析結果を置く場所。

- ファイル名は「日付＋テーマ名」（例：`2026-09-03_プレミアプロプラグイン調査.md`）
- 保存は毎回自動じゃなく、頼まれた時だけ
- Obsidianでこのフォルダ（`第二の脳`）をVaultとして開けば、そのままメモとして見れる
- 更新を受け取りたい時は、ローカルのPowerShellで以下を実行：
  ```
  cd ~/Desktop/claude
  git pull
  ```
